# data

- `generated/` — output of the synthetic data generator (gitignored; not committed). Created by running the generator locally.
- `fixtures/` — a small, explicit set of committed synthetic records used by the automated test suite (task group 5).

See `openspec/changes/add-solvia-foundation-mvp/specs/synthetic-data/spec.md`.
