# rag

Regulatory RAG (ingestion, chunking, indexing of Open Finance / BACEN /
LGPD documents, `knowledge_agent`, LangServe chain).

Scaffold only in `add-solvia-foundation-mvp` — implemented in
`add-regulatory-rag` (see `proposal.md` — Out of scope).
