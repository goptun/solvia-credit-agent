# infra

Deployment-related configuration (reverse proxy vhost templates, CI/CD
deploy scripts) for the production VPS deployment.

Scaffold only in `add-solvia-foundation-mvp` — implemented in
`add-vps-deploy` (see `proposal.md` — Out of scope). Local development's
`docker-compose.yml` lives at the repository root (task group 10).
