# evals

LangFuse evaluation datasets and CI eval runner (router accuracy,
tool-call correctness, RAG faithfulness, compliance violation rate).

Scaffold only in `add-solvia-foundation-mvp` — implemented in
`add-llm-evals` (see `proposal.md` — Out of scope).
