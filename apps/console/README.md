# apps/console

Streamlit agent console (conversation view, graph state, trace link).

Scaffold only in `add-solvia-foundation-mvp` — this directory exists to
establish the monorepo layout; the console itself is wired up in
`add-human-handoff` (see `proposal.md` — Out of scope).
