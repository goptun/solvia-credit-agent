# services/crm_mock

Mock CRM microservice used by the human-handoff flow.

Scaffold only in `add-solvia-foundation-mvp` — this directory exists to
establish the monorepo layout; the service itself is implemented in
`add-human-handoff` (see `proposal.md` — Out of scope).
